name: Sprint 8E AI provider execution recovery gate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "backend/migrations/0017_ai_provider_execution_journal.sql"
      - "backend/src/modules/ai-provider-execution/**"
      - "backend/src/modules/ai-provider/**"
      - "backend/src/modules/ai-operations/**"
      - "backend/src/modules/ai-automation/**"
      - "backend/src/queue/ai-discovery-automation-worker.ts"
      - "backend/src/ai-provider-execution-cli.ts"
      - "backend/src/ai-automation-worker.ts"
      - "backend/src/ai-automation-config.ts"
      - "backend/test/ai-provider-execution-*.test.ts"
      - "backend/test/openai-responses-provider.test.ts"
      - "backend/test/execute-ai-discovery-provider-*.test.ts"
      - "backend/test/execute-policy-governed-ai-discovery-run.test.ts"
      - "backend/test/ai-discovery-automation-*.test.ts"
      - "backend/test/ai-operations-*.test.ts"
      - "backend/test/migration.test.ts"
      - "backend/package.json"
      - "backend/package-lock.json"
      - "tests/ai-provider-execution-recovery-contract.test.mjs"
      - "tests/ai-discovery-automation-contract.test.mjs"
      - "tests/ai-operations-policy-contract.test.mjs"
      - "tests/ai-provider-execution-contract.test.mjs"
      - "tests/guarded-ai-discovery-contract.test.mjs"
      - "docs/runbooks/ai-provider-execution-recovery.md"
      - "docs/superpowers/specs/2026-08-20-ai-provider-execution-recovery-design.md"
      - "docs/superpowers/plans/2026-08-20-ai-provider-execution-recovery.md"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/sprint-8e-ai-provider-execution-recovery.yml"

permissions:
  contents: read

concurrency:
  group: sprint-8e-ai-provider-execution-recovery-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: verify durable AI provider execution recovery
    runs-on: ubuntu-latest
    timeout-minutes: 45
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: hai_dau_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d hai_dau_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      NODE_ENV: test
      TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/hai_dau_test
      TEST_REDIS_URL: redis://127.0.0.1:6379
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
          cache-dependency-path: |
            package-lock.json
            backend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci --cache /tmp/aram-root-npm-cache

      - name: Install backend dependencies
        run: npm --prefix backend ci --cache /tmp/aram-backend-npm-cache

      - name: Sprint 8E repository contract
        run: npm run test:ai-provider-execution-recovery

      - name: Backend typecheck
        run: npm --prefix backend run typecheck

      - name: Sprint 8E focused tests
        run: >-
          cd backend && node --import tsx --test --test-concurrency=1
          test/ai-provider-execution-*.test.ts
          test/openai-responses-provider.test.ts

      - name: Backend full tests
        run: npm --prefix backend test

      - name: Sprint 8D automation contract
        run: npm run test:ai-discovery-automation

      - name: Sprint 8C policy and budget contract
        run: npm run test:ai-operations-policy

      - name: Sprint 8B provider execution contract
        run: npm run test:ai-provider-execution

      - name: Sprint 8A guarded AI discovery contract
        run: npm run test:guarded-ai-discovery

      - name: Sprint 7A monitoring regression
        run: npm run test:post-publication-monitoring

      - name: Sprint 7B feedback regression
        run: npm run test:feedback-intake

      - name: Sprint 7C operator regression
        run: npm run test:operator-surface

      - name: Frontend lint
        run: npm run lint

      - name: Backend build
        run: npm --prefix backend run build

      - name: Repository cleanliness
        shell: bash
        run: |
          set -euo pipefail
          git diff --check
          status="$(git status --short)"
          if [[ -n "$status" ]]; then
            echo "Generated files changed the checkout:"
            echo "$status"
            exit 1
          fi

      - name: Deployment and secret guard
        shell: bash
        run: |
          set -euo pipefail
          workflow=".github/workflows/sprint-8e-ai-provider-execution-recovery.yml"
          executable="$RUNNER_TEMP/sprint-8e-before-guard.yml"
          sed '/- name: Deployment and secret guard/,$d' "$workflow" > "$executable"
          ! grep -Eq '(contents|packages|pages|id-token):[[:space:]]*write' "$executable"
          for token in \
            "railway"" up" \
            "wrangler"" deploy" \
            "docker"" push" \
            "kube""ctl" \
            "terra""form" \
            "pu""lumi"; do
            ! grep -Fqi "$token" "$executable"
          done
          provider_secret="OPENAI_""API_KEY"
          scheduler_activation="AI_DISCOVERY_SCHEDULER_""ENABLED=true"
          ! grep -Fq "$provider_secret" "$executable"
          ! grep -Fq "$scheduler_activation" "$executable"
