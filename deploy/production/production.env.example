# Reference-variable examples only. Do not commit resolved values or secrets.
NODE_ENV=production
HOST=0.0.0.0
BACKEND_PORT=3001
GATEWAY_PORT=8080
BACKEND_ORIGIN=http://${{backend.RAILWAY_PRIVATE_DOMAIN}}:3001
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}
