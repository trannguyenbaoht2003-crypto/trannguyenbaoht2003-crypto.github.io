# Local/CI staging defaults only. Do not reuse these values in production.
STAGING_PORT=8080
POSTGRES_DB=hai_dau_staging
POSTGRES_USER=hai_dau
POSTGRES_PASSWORD=local-staging-only
FEEDBACK_INTAKE_ENABLED=false
